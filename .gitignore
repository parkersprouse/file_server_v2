# Logs
logs
*.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/tailwind.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo
/target

# Config
.env
config.toml

# Caddy's local CA + issued certs (see Caddyfile's `storage` block)
.caddy

# Custom Caddy builds with the layer4 plugin — built locally by bin/build-caddy.sh
# rather than committed. (The build script and its launcher stay tracked.)
/bin/*
!/bin/build-caddy.sh
!/bin/run-caddy.mjs
