{
  "name": "web_file_browser",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22",
    "pnpm": "^10"
  },
  "scripts": {
    "start": "concurrently --kill-others --names \"Browser Client\",\"Browser Server\" --prefix-colors blue,yellow \"pnpm run client\" \"pnpm run server\"",
    "client": "cd client && pnpm run build && serve -l 3000 --single dist",
    "server": "cd server && cargo run -r"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
